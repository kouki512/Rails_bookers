    <div id="notice">
        <% if flash[:notice] %>
            <div class="flash">
                 <%= flash[:notice] %>
            </div>
    <% end %>
        
    </div>
<div class="show-box show-title">
        <h4>Title:</h4>
        <%= @book.title %>
    
</div>

<div class="show-box show-body">
    <h4>Body:</h4>
    <%= @book.body %>
</div>
<%= link_to "Edit", edit_book_path(@book.id) %> |
<%= link_to "Back",books_path %>

